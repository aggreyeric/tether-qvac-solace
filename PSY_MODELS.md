# QVAC — Psy / Psyche Model Search

**Date:** 2026-06-21 (updated: registry source file checked)
**Task:** Determine whether Tether QVAC exposes or supports any "Psy" or "Psyche" models.

## TL;DR

**No "Psy" or "Psyche" models found — in the docs OR in the full registry source file.**
The term does not appear in the documentation body text, the navigation, the API
reference, the model-constant identifiers, or anywhere in the 1 MB / 750-entry model
registry source file.

## Pages Checked

1. **https://qvac.tether.io** — redirects to `https://docs.qvac.tether.io/introduction/`
   (the JS/TS SDK introduction page). The bare `qvac.tether.io` host serves the docs,
   so "both" URLs resolve to the same docs site.
2. **https://docs.qvac.tether.io** — docs landing/home page.
3. (Extra, to be thorough about the full model list)
   - `https://docs.qvac.tether.io/reference/api/` — API reference (85 occurrences of
     the word "model").
   - `https://docs.qvac.tether.io/ai-capabilities/text-generation/` — LLM model list.

## Search Method

For each page I ran an in-browser scan over `document.body.innerText`:

- Word-boundary regex `\b\w*psy\w*\b` (case-insensitive) — **0 matches** on every page.
- Word-boundary regex `\b\w*psyche\w*\b` (case-insensitive) — **0 matches** on every page.
- Counted occurrences of the word `model`.
- Extracted model-constant identifiers (`<NAME>_Q<N>_<N>` pattern) and named model
  families (Llama, Qwen, Mistral, Gemma, DeepSeek, Phi, GPT, Psy, Psyche, ...).

No Psy/Psyche tokens surfaced anywhere.

## What Models QVAC *Does* Support (observed)

From the API reference and text-generation page, the model constants currently
documented are:

| Constant | Family / Capability |
| --- | --- |
| `LLAMA_3_2_1B_INST_Q4_0` | Llama 3.2 — text generation (LLM) |
| `QWEN3_600M_INST_Q4` | Qwen3 600M — text generation (LLM) |
| `QWEN3_1_7B_INST_Q4` | Qwen3 1.7B — text generation (LLM) |
| `PARAKEET_TDT_0_6B_V3_Q8_0` | NVIDIA Parakeet — transcription (ASR) |
| `TTS_T3_TURBO_EN_CHATTERBOX_Q8_0` | Text-to-Speech (English) |
| `TTS_MULTILINGUAL_SUPERTONIC2_Q8_0` | Text-to-Speech (multilingual) |

The SDK does **not** ship models built-in; these constants are just pointers into
QVAC's distributed model registry. The introduction page notes the full index lives
in the registry (see `modelRegistryList()`, `modelRegistrySearch()`,
`modelRegistryGetModel()` in the API reference), and the source index is at
`https://github.com/tetherto/qvac/blob/main/packages/sdk/models/registry/models.ts`.

> Note: I did not fetch the GitHub registry source file (out of scope for "search the
> two pages"). If a Psy/Psyche model exists in the registry but is not yet referenced
> in the docs, that GitHub file is where it would be found.

---

## Update 2026-06-21 — Registry Source File Checked (this is where Psy would live)

The note above flagged the GitHub registry source as the one place a Psy model could
hide without showing up in the docs. **It has now been checked. Still no Psy.**

### URL fetched

`https://raw.githubusercontent.com/tetherto/qvac/main/packages/sdk/models/registry/models.ts`

### Result

| Check | Result |
| --- | --- |
| HTTP / fetch | ✅ Succeeded — real file, not a 404 |
| File size | **1,069,801 chars** (~1 MB) |
| File header | `// THIS FILE IS AUTO-GENERATED BY models/update-models // DO NOT MODIFY MANUALLY` |
| `modelId:` entries | **750** real model blobs |
| `\b\w*psy\w*\b` (word-boundary, case-insensitive) | **0 matches** |
| plain substring `"psy"` (case-insensitive) | **0 matches** |
| `"psyche"` / `"psyk"` / `"psycle"` / `"psyc"` | **0 matches each** (indexOf = -1) |

### Proof the file is the genuine registry (so "0 matches" actually means absent)

Other model families show large counts, confirming this is a populated registry and
not an error page:

| Token | Occurrences |
| --- | --- |
| `tts` | 614 |
| `whisper` | 263 |
| `qwen` | 211 |
| `parakeet` | 162 |
| `gemma` | 145 |
| `llama` | 120 |
| `phi` | 23 |
| `gpt` | 20 |
| `psy` / `psyche` / `mistral` / `deepseek` | **0 each** |

Sample `modelId` values from the 750 entries: `bci-embedder.bin`,
`wan2.1-i2v-14b-720p-Q4_K_M.gguf`, `stable-diffusion-v2-1-Q8_0.gguf`,
`umt5_xxl_fp16.safetensors`, `tiny_en_acft_q8_0.bin`, etc.

### Interpretation

The registry ships diffusion (Wan2.1, Stable Diffusion), ASR/Whisper, TTS, BCI
embedders, and LLMs (Llama, Qwen, Gemma, Phi, GPT) — **but no Psy / Psyche model,
under any spelling.** Both the word-boundary regex and the plain substring scan agree
on zero hits, and the file is verifiably the real 750-entry index.

## Conclusion

No Psy or Psyche models are mentioned on `https://qvac.tether.io`,
`https://docs.qvac.tether.io`, **or in the full 750-entry model registry source file
on GitHub** (`packages/sdk/models/registry/models.ts`) as of 2026-06-21. If Tether has
announced a "Psy" model, it is not present in the QVAC documentation or the QVAC SDK
model registry.

### Recommendation for the hackathon submission

The QVAC judges score "creative use of Psy models," but no Psy model exists anywhere
in the public QVAC surface we can reach. Options to raise with HICLAW_MANAGER / Eric:
1. Do not claim Psy usage in the submission (avoids a false claim the judges can debunk).
2. Search for a separate "Psy" product outside QVAC (e.g. Tether AI / app store) — out
   of scope of this check; would need a different task.
3. Treat "Psy" as a *category* (psychology/therapy assistant) rather than a specific
   model name, and build on top of an available QVAC LLM (Qwen3, Llama 3.2).

---

## Update 2026-06-21 (2) — Independent re-verification (browser_use + eval)

Repeated the registry-source check using the exact method requested by
HICLAW_MANAGER: `browser_use` open of the raw URL, then `action=eval` running
`document.body.innerText.match(/psy/gi)`.

### URL & method

- `https://raw.githubusercontent.com/tetherto/qvac/main/packages/sdk/models/registry/models.ts`
- Opened via `browser_use action=open`; then `action=eval`:
  `document.body.innerText.match(/psy/gi)`
- IIFE wrapper required because the eval sandbox rejects bare top-level `const`
  statements (`SyntaxError: Unexpected token 'const'`). Wrapped as
  `(() => { ... })()` and it ran cleanly.

### Result — still zero "psy"

| Check | Result |
| --- | --- |
| HTTP / fetch | ✅ Succeeded |
| Body text length | **1,069,801 chars** (~1 MB) — matches prior reading |
| `text.match(/psy/gi)` | **`null`** (0 matches) |
| `text.match(/psych/gi)` | **`null`** (0 matches) |
| Total `name:` fields | 1,203 |
| **Unique** model names | **797** |

Broadened to related medical / cognitive domain terms to flag the closest
substitutes (in case "Psy" is meant loosely as a psychology/therapy category):

| Term (case-insensitive) | Occurrences in body text |
| --- | --- |
| `psy` | **0** |
| `psych` | **0** |
| `medic` | 0 |
| `medgemma` | 40 |
| `brain` | 0 |
| `bci` | 20 |
| `mental` | 0 |
| `health` | 0 |
| `clinic` | 0 |
| `neuro` | 0 |

### Closest domain-relevant models (medical / brain) — useful for a "psy" category build

No model is literally named "Psy", but these are the nearest cognitive/medical
offerings and would be the natural backbone for a psychology/therapy agent built
on QVAC:

**Medical LLM — MedGemma 4B IT** (Google's medical-tuned Gemma):
- `MEDGEMMA_4B_IT_Q4_1`, `MEDGEMMA_4B_IT_Q8_0`
- Sharded: `MEDGEMMA_4B_IT_Q4_1_SHARD` / `MEDGEMMA_4B_IT_Q8_0_SHARD` (+ gguf shard
  files `medgemma-4b-it-Q4_1-0000N-of-00005.gguf`, `...Q8_0-0000N-of-00005.gguf`)
- Tensors: `MEDGEMMA_4B_IT_Q4_1_TENSORS`

**Brain-Computer Interface embedders (Tether-specific):**
- `BCI_EMBEDDER`, `BCI_WINDOWED`

### Conclusion (unchanged)

The independent re-check confirms: **no "psy" / "psyche" model exists anywhere in
the QVAC SDK model registry** (0 matches by both word-boundary and plain-substring
search, across a verifiably-populated 797-unique-model index). If the hackathon
brief rewards "creative use of Psy models," there is no literal Psy model to use.
The closest available building blocks are **MedGemma 4B** (medical/clinical LLM) and
the **BCI** embedders; otherwise treat "Psy" as a *use case* on top of a general
LLM (Qwen3 / Llama 3.2).

---

## Re-verification (browser fetch, 2026-06-21)

GitHub registry checked — no Psy models found (2026-06-21)

- **Source:** `https://raw.githubusercontent.com/tetherto/qvac/main/packages/sdk/models/registry/models.ts`
- **Method:** fetched via browser, ran `document.body.innerText.toLowerCase().includes("psy")` → **false**
- **File size:** 1,069,801 chars (full auto-generated registry, real content — not a 404)
- **Substring scan:** `psy` → **0 occurrences** anywhere in the file
- **Conclusion:** confirms prior finding. No Psy / Psyche model exists in the QVAC model registry.
